---
description: Run flutter analyze and tests to verify code quality
---

// turbo
1. Run static analysis:
```bash
export PATH="$PATH:/opt/homebrew/bin:/Users/vaisakhprakash/flutter/bin" && cd /Users/vaisakhprakash/Developer/Code/VPDevPortal/gymai/apps/app && flutter analyze --no-pub
```

// turbo
2. Run unit and widget tests:
```bash
export PATH="$PATH:/opt/homebrew/bin:/Users/vaisakhprakash/flutter/bin" && cd /Users/vaisakhprakash/Developer/Code/VPDevPortal/gymai/apps/app && flutter test
```
